{
  "name": "shared",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "shared"
  ],
  "scripts": {
    "dev": "bun run --filter=\"!shared\" dev",
    "docker:down": "docker compose down -v",
    "docker:up": "docker compose up -d"
  }
}
